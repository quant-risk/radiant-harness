---
name: diagrams
description: High-level architecture diagrams (Mermaid). Pull when designing or reviewing architecture. Generated by /diagramar.
alwaysApply: false
---

# Architecture Diagrams

> High level (C4 L1-L2 + bounded context map). Generated/updated by `/diagramar`.
> Renders on GitHub and in Claude Code. Keep in sync with `context-map.md` and `design.md`s.

## 1. System context (C4 L1)
> The system at the center, with personas and external systems.

```mermaid
flowchart LR
  user([Persona]) --> sys[System]
  sys --> ext[External System]
```

## 2. Containers (C4 L2)
> The running pieces (UI, services, data, queues) and how they talk.

```mermaid
flowchart LR
  UI[App / UI] --> API[API / Service]
  API --> DB[(Database)]
  API --> Q[[Queue]]
  API --> EXT[External System]
```

## 3. Bounded context map (DDD)
> The system's contexts and the relationship pattern between them.

```mermaid
flowchart TB
  A[Context A - core] -->|ACL| B[Context B]
  A -->|Customer/Supplier| C[Context C]
```
