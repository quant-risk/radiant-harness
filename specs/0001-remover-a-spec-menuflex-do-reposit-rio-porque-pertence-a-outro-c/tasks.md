# tasks.md — 0001-remover-a-spec-menuflex-do-reposit-rio-porque-pertence-a-outro-c

> Generated by `radiant-harness` self-driven mode (v3.6.0+).
> Filled by the host agent as the audit trail for removing an unrelated user case.

## Tasks

1. Remove the tracked MenuFlex spec files from `specs/`.
2. Scan the repository for remaining MenuFlex references outside this cleanup audit trail.
3. Keep the broader radiant-harness backlog as a separate follow-up.

## Gates (suggested)

- `go build ./...` (or stack equivalent) → PASS
- `go test ./...` (or stack equivalent) → PASS
- `./scripts/run.sh` → PASS, confirms no MenuFlex references remain outside this cleanup audit trail.

---
Host-agent note: this is a cleanup-only task; no product behavior is intentionally changed.
